package io.github.joaovictor.smartsupport.entity.enums;

/** Níveis de prioridade, em ordem crescente de severidade ({@code ordinal}). */
public enum TicketPriority {
    LOW,
    MEDIUM,
    HIGH,
    URGENT
}
